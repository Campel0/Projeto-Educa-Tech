<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale:1.0">
    <title>Meu Perfil - EducaTech</title>
    <link rel="icon" href="/img/logo-educatech.svg" type="image/svg+xml">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>

    <div id="conteudo-perfil" class="container content-area">
        <h2>Meu Perfil</h2>
        
        <div class="form-group avatar-section">
            <label>Avatar:</label>
            <div class="avatar-placeholder">
                <span id="avatar-iniciais"><%= username.substring(0, 2) %></span>
            </div>
            <button class="btn btn-small">Trocar Avatar</button>
        </div>

        <div class="form-group">
            <label for="perfil-nome">Nome (Usuário):</label>
            <input type="text" id="perfil-nome" value="<%= username %>" readonly>
        </div>

        <div class="form-group-inline" style="display: flex; gap: 20px;">
            <div class="form-group" style="flex: 1;">
                <label for="perfil-idade">Idade:</label>
                <input type="text" id="perfil-idade" value="<%= idade %> anos" readonly>
            </div>
            <div class="form-group" style="flex: 1;">
                <label for="perfil-turma">Turma:</label>
                <input type="text" id="perfil-turma" value="<%= turma %>" readonly>
            </div>
        </div>

        <div class="profile-stats" style="display: flex; justify-content: space-around; background: #f9f9f9; border-radius: 8px; padding: 15px; margin-bottom: 20px;">
            <div class="stat-item" style="text-align: center;">
                <span style="font-size: 1.5rem; font-weight: bold; color: #ff6347;"><%= points %></span>
                <span style="font-size: 0.9rem; color: #555;">Pontos 🌟</span>
            </div>
            <div class="stat-item" style="text-align: center;">
                <span style="font-size: 1.5rem; font-weight: bold; color: #ff6347;"><%= level %></span>
                <span style="font-size: 0.9rem; color: #555;">Nível 🚀</span>
            </div>
            <div class="stat-item" style="text-align: center;">
                <span style="font-size: 1.5rem; font-weight: bold; color: #ff6347;"><%= games %></span>
                <span style="font-size: 0.9rem; color: #555;">Jogos 🎮</span>
            </div>
        </div>

        <div class="form-group">
            <label for="perfil-descricao">Minha Descrição:</label>
            <textarea id="perfil-descricao" name="perfil-descricao" rows="4" placeholder="Fale um pouco sobre você..."><%= descricao %></textarea>
        </div>
        
        <div class="user-actions">
            <button id="botao-salvar-perfil" class="btn">Salvar Alterações</button>
            <a href="/logout" id="botao-sair" class="btn btn-secondary">Sair</a>
        </div>
        </div>

</body>
</html>